<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
  <div class="container">
    <!-- Logo / Brand -->
    <a class="navbar-brand fw-bold text-uppercase" href="/">
      <i class="bi bi-box-seam me-2"></i> Product Manager
    </a>

    <!-- Nút toggle cho mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Menu -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link <%= (title==='Home'?'active':'') %>" href="/">
            <i class="bi bi-house-door me-1"></i> Home
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= (title==='Suppliers'?'active':'') %>" href="/suppliers">
            <i class="bi bi-people me-1"></i> Suppliers
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= (title==='Products'?'active':'') %>" href="/products">
            <i class="bi bi-boxes me-1"></i> Products
          </a>
        </li>
      </ul>

      <!-- Trạng thái user -->
      <ul class="navbar-nav ms-3">
        <% if (user) { %>
          <li class="nav-item">
            <span class="nav-link text-info fw-semibold">
              <i class="bi bi-person-circle me-1"></i> Hi, <%= user.username %>
            </span>
          </li>
          <li class="nav-item">
            <form method="POST" action="/auth/logout" class="d-inline">
              <button class="btn btn-outline-light btn-sm">
                <i class="bi bi-box-arrow-right me-1"></i> Logout
              </button>
            </form>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link fw-semibold" href="/auth/login">
              <i class="bi bi-box-arrow-in-right me-1"></i> Login
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link fw-semibold" href="/auth/register">
              <i class="bi bi-person-plus me-1"></i> Register
            </a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<!-- Bootstrap icons -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
